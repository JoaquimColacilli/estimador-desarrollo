import { bootstrapApplication } from '@angular/platform-browser';
import { EstimadorComponent } from './app/estimador/estimador.component';

bootstrapApplication(EstimadorComponent).catch((err) => console.error(err));
